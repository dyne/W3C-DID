Rule caller restroom-mw

Given I have a valid redis connection on 'redis://localhost:6379'
Given I read from redis the data under the keys containing 'did:dyne:' and save the output into 'result'

Given I have a 'string array' named 'result'

When I create the 'string array' named 'did_documents'
Foreach 'did_document' in 'result'
When I pickup from path 'did_document.didDocument'
When I move 'didDocument' in 'did_documents'
Endforeach
Then print the 'did_documents'